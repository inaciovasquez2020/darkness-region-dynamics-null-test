name: zlg-mc5-sibling-kernel-extended
on:
  push:
    branches:
      - scratch/zlg-mc4-finite-20260828
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Verify rank-six sibling kernel through dimension ten
        run: python3 scratch/zlg_mc5_sibling_kernel_extended.py | tee /tmp/zlg_mc5_sibling_kernel_extended.log
      - name: Upload verifier log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zlg-mc5-sibling-kernel-extended
          path: /tmp/zlg_mc5_sibling_kernel_extended.log
          if-no-files-found: error

name: zlg-mc5-sibling-common-factor

on:
  push:
    branches:
      - scratch/zlg-mc4-finite-20260828
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify generic common-factor closure
        working-directory: scratch
        run: python3 zlg_mc5_sibling_common_factor.py
